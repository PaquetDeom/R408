package fr.paquet.test.projet;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

import fr.paquet.projet.Projet;
import fr.paquet.projet.Responsable;

public class ResponsableTest {

	/**
	 * private Responsable getResponsable() throws Exception { DonneesTest dN = new
	 * DonneesTest(); return dN.getResp(); }
	 * 
	 * @Test public void testResponsableNom() { try {
	 * 
	 *       assertTrue(getResponsable().getNom().equals("BOUCHARD"));
	 * 
	 *       } catch (Exception e) { e.printStackTrace(); } }
	 * 
	 * @Test public void testResponsablePrenom() { try {
	 * 
	 *       assertTrue(getResponsable().getPrenom().equals("Gerard"));
	 * 
	 *       } catch (Exception e) { e.printStackTrace(); } }
	 * 
	 * @Test public void testResponsableProjets() { try {
	 * 
	 *       Responsable resp = new Responsable(); Responsable resp1 = new
	 *       Responsable();
	 * 
	 *       Projet pro1 = new Projet(); Projet pro2 = new Projet();
	 * 
	 *       pro1.setResp(resp); pro2.setResp(resp);
	 * 
	 *       assertTrue(resp.getProjets().size() == 2);
	 * 
	 *       pro2.setResp(resp1); assertTrue(resp.getProjets().size() == 1);
	 *       assertTrue(resp1.getProjets().size() == 1);
	 * 
	 *       } catch (Exception e) { e.printStackTrace(); } }
	 */

}
